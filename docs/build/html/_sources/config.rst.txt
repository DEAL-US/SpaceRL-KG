Configuration
=============
**Config** holds the Experiment and Test classes which are just container classes.

.. autoclass:: model.config.Experiment
.. autoclass:: model.config.Test

It also contains a function to pull the experiment/test list as well as the config dict.

.. autofunction:: model.config.get_config 
